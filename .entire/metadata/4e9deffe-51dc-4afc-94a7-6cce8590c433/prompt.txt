/fnr-new-task # Не видно, какая версия сервиса отвечает

Сервис не говорит, какая его версия отвечает на запрос.

`GET /healthz` возвращает `{"status":"ok","uptime_sec":123}` — по этому ответу нельзя понять, какая сборка сейчас развёрнута. При разборе инцидента на стенде это первое, что хочется узнать, и приходится лезть в контейнер.

Версия у сервиса есть — в `package.json`, поле `version`. В коде она не используется нигде.

## Обсуждение
**@poh-harness-demo[bot] (2026-09-01):**
## 📋 БФТ (быстрый проход)

**Цель:** При разборе инцидентов сложно определить развёрнутую сборку без доступа к контейнеру — добавляем вывод версии сервиса в ответ эндпоинта /healthz.

**How to demo:**
1. Отправить запрос GET /healthz.
2. Проверить, что в ответе (в теле JSON) присутствует версия сервиса.

**Открытые вопросы:**
* Какое наименование поля использовать для версии в JSON-ответе (например, version или app_version)?

**Границы:** Добавление поля с версией в ответ эндпоинта /healthz (изменение способа хранения версии не входит).

**Документация:**
* Issue po-helper-org/poh-demo-checkout#165

**Ключевые требования:**

| ID | Сейчас (AS IS) | После (TO BE) | Связанные | Источник |
|---|---|---|---|---|
| БТ-1 | Для определения версии сервиса при разборе инцидента необходимо лезть в контейнер. | Версия сервиса доступна в ответе на запрос здоровья сервиса. |  | При разборе инцидента на стенде это первое, что хочется узнать, и приходится лезть в контейнер. |
| ФТ-1 | GET /healthz возвращает {"status":"ok","uptime_sec":123} без информации о сборке. | GET /healthz возвращает объект JSON, содержащий информацию о версии сервиса. |  | GET /healthz возвращает {"status":"ok","uptime_sec":123} — по этому ответу нельзя понять, какая сборка сейчас развёрнута. |
| ФТ-2 | Значение версии хранится в package. и не используется в коде. | Значение версии для ответа берётся из поля version файла package.. | ФТ-1 | Версия у сервиса есть — в package., поле version. В коде она не используется …[обрезано]

**@poh-harness-demo[bot] (2026-09-01):**
## Приоритет: P1

- Impact: 2/5, Time criticality: 1/5, Risk reduction: 1/5
- OKR alignment: unrelated (×0.5)
- Effort: 1/10
- Score = (4 × 0.5) / 1 = 2.0

**Кто исполняет:** 
**Риски:** —
**Влияние на цели:** 

<!-- issue-agent -->

**@poh-harness-demo[bot] (2026-09-01):**
🔍 Взял `research-me` в работу — запускаю автономный анализ через SA-helper.

Прогон занимает несколько минут: артефакты появятся в ветке `research/issue-165`, а сводка — следующим комментарием.

<!-- issue-agent -->