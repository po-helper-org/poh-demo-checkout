было — запрос файла из каталога-соседа, имя которого начинается с имени каталога статики (например `static-secret/keys.json`), отдаёт содержимое этого файла; стало — такой запрос получает 404 и содержимое файла не отдаётся.