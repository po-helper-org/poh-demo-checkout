# Довести PR #35 по замечаниям ревью

Круг 3 из 3.

## Замечания ревьюера

Preparing review...

**[PR Description](https://github.com/po-helper-org/poh-demo-checkout/pull/35)** updated to latest commit (https://github.com/po-helper-org/poh-demo-checkout/commit/6fad5ae04038f067ae132cb34baa43238ae503d5)

## PR Reviewer Guide 🔍

#### (Review updated until commit https://github.com/po-helper-org/poh-demo-checkout/commit/af913aa5cb62a815dfc24c88d9ccb1d8ef84835d)


Here are some key observations to aid the review process:

<table>
<tr><td>

**🎫 Ticket compliance analysis 🔶**



**[29](https://github.com/po-helper-org/poh-demo-checkout/issues/29) - Partially compliant**

Compliant requirements:

- Add packaging fee based on number of packages (5 units per package, 50₽ per package)
- Number of packages = ceil(total units / 5)
- Response should include new `packages` field
- Packaging fee should be added to `delivery` (no separate field)
- `goods` should not change
- `total = goods + delivery`
- Free delivery from 3000₽ only waives the base 300₽ fee, not packaging fee
- No new dependencies

Non-compliant requirements:

- Order with no items should still be an error

Requires further human verification:

- No items requiring verification beyond what's evident in the code



</td></tr>
<tr><td>⏱️&nbsp;<strong>Estimated effort to review</strong>: 2 🔵🔵⚪⚪⚪</td></tr>
<tr><td>🧪&nbsp;<strong>PR contains tests</strong></td></tr>
<tr><td>🔒&nbsp;<strong>No security concerns identified</strong></td></tr>
<tr><td>⚡&nbsp;<strong>Recommended focus areas for review</strong><br><br>

<details><summary><a href='https://github.com/po-helper-org/poh-demo-checkout/pull/35/files#diff-8e0065ef5c21772c634e61a13df0c66a130bacebce2ac5f1b418cfc4775d1b97R69-R74'><strong>Missing Empty Order Validation</strong></a>

The ticket requires that "Заказ без позиций по-прежнему ошибка" (Order without items should still be an error), but the `quote` function does not validate that the items array is non-empty. When called with an empty array, it returns `{"goods":0,"packages":0,"delivery":0,"total":0}` instead of throwing an error. This allows processing of empty orders which violates the specified requirement.
</summary>

```txt
export function quote(items) {
  const goods = subtotal(items);
  const packages = countPackages(items);
  const delivery = deliveryFee(goods) + packagingFee(packages);
  return { goods, packages, delivery, total: goods + delivery };
}
```

</details>

</td></tr>
</table>

**[Persistent review](https://github.com/po-helper-org/poh-demo-checkout/pull/35#issuecomment-5339504097)** updated to latest commit https://github.com/po-helper-org/poh-demo-checkout/commit/7972029063ba12ac92e80d31dabe2469fc563dae

**[Persistent review](https://github.com/po-helper-org/poh-demo-checkout/pull/35#issuecomment-5339504097)** updated to latest commit https://github.com/po-helper-org/poh-demo-checkout/commit/7972029063ba12ac92e80d31dabe2469fc563dae

**[Persistent review](https://github.com/po-helper-org/poh-demo-checkout/pull/35#issuecomment-5339504097)** updated to latest commit https://github.com/po-helper-org/poh-demo-checkout/commit/af913aa5cb62a815dfc24c88d9ccb1d8ef84835d

## Что делать

Сначала **реши по каждому замечанию, стоит ли оно правки**. Автоматический
ревьюер находит к чему придраться всегда — это его работа. Твоя работа —
отличить то, что улучшит код, от того, что просто сожжёт круг.

**Исправлять:** неверное поведение, необработанный случай, расхождение с
требованиями задачи, отсутствующий тест на новую логику, настоящая уязвимость.

**Отклонять:**
- стилистические придирки, не меняющие поведения;
- требования того, что уже сделано ВНЕ диффа — ревью видит только дифф и
  регулярно требует существующего; проверь по коду, прежде чем исправлять;
- пожелания «на будущее» и архитектурные советы, не относящиеся к задаче этого
  PR — им место в отдельной задаче, а не здесь;
- повтор замечания, отклонённого на прошлом круге, без новых доводов.

Затем:

1. **Исправь принятое.** Только в границах уже написанного в этом PR. Это
   доведение, а не разработка: новая функциональность сюда не добавляется.
2. **Если принятого нет — НЕ МЕНЯЙ НИЧЕГО.** Пустой результат это законный
   исход круга. Не правь ради того, чтобы что-то изменить: косметика создаст
   видимость работы и уведёт следующий круг в спор с ревьюером.
3. **Запиши разбор в файл `.verdict.md`** — по строке на замечание:
   `принято: <что и как исправил>` либо `отклонено: <почему>`. Файл обязателен
   даже когда всё отклонено: молчаливое «ничего не изменилось» неотличимо от
   сломанного прогона.
4. **Тесты.** Прогоняй проверки проекта; красный код в PR не отдаём.
5. **Коммитить самому не надо** — коммит и пуш делает контур после тебя.
